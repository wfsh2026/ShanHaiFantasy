/// <summary>
/// 角色属性类型定义。
/// </summary>
public enum RoleAttrType {
    HP = 0,
    MP = 1,
}

/// <summary>
/// 角色属性操作类型定义。
/// </summary>
public enum RoleAttrOperationType {
    Add = 0,
    Reduce = 1,
}
