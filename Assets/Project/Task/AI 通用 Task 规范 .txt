# TASK_通用模板_V1

> 用途：
> 本模板用于《宗门大比》项目中所有 TASK 的统一编写与执行。
> 它不是普通需求文档，而是面向 AI 稳定施工的工程执行模板。
> 每个 TASK 必须基于本模板填写，确保输入明确、边界明确、输出可追踪、测试可执行、验收可判断。

---

# 1. TASK 基本信息

## 1.1 基础信息
- TASK 编号：
- TASK 名称：
- 文档版本：
- 当前状态：未开始 / 编写中 / 开发中 / 待测试 / 待验收 / 已完成
- 优先级：P0 / P1 / P2
- 所属阶段：主链 / 养成 / 模块 / 工具 / 测试 / UI / 配置 / AI基建
- 所属系统：
- 负责人：
- 创建日期：
- 最后更新日期：

## 1.2 依赖与影响
- 前置依赖 TASK：
- 后续衔接 TASK：
- 关联文档：
- 影响模块：
- 影响场景：
- 影响配置：
- 影响 UI：
- 影响网络同步：
- 是否涉及 Base：
- 是否涉及主链：
- 是否涉及冻结区：
- 是否需要单独授权后才能施工：是 / 否

---

# 2. 任务目标与范围

## 2.1 本次任务目标
- 
- 
- 

## 2.2 本次必须完成
- 
- 
- 

## 2.3 本次可选完成
- 
- 
- 

## 2.4 本次明确不做
- 
- 
- 

## 2.5 完成标志
- 
- 
- 

---

# 3. 任务背景与上下文

## 3.1 当前任务背景
- 当前为什么需要做这个 TASK：
- 它在整个项目中的位置：
- 它解决的核心问题是什么：
- 它与前置 TASK 的关系：
- 它与后续 TASK 的关系：

## 3.2 当前架构上下文
- 当前合法主链：
- 当前任务所属层：
- 当前必须接入的系统：
- 当前必须复用的管理器 / 入口：
- 当前禁止绕开的系统入口：
- 当前不得破坏的初始化顺序：
- 当前不得修改的冻结区说明：

## 3.3 当前版本边界提醒
- 当前版本只要求做到：
- 当前版本不要求做到：
- 当前版本暂不支持：

---

# 4. AI 开发输入

> 本章节是给 AI 的直接施工输入，必须尽量写清楚、写死、写窄，避免 AI 自由发挥。

## 4.1 本次 AI 必读资料
1. 继承总文档：
2. 总开发总案：
3. TASK 总表：
4. 当前 TASK 执行包：
5. AI 开发规范文档：
6. AI 整体架构设计文档：
7. 当前模块补充规范：
8. 当前相关脚本：
9. 当前相关测试场景：
10. 当前相关配置：

## 4.2 本次允许改动范围
### 4.2.1 允许新增
- 允许新增目录：
- 允许新增脚本：
- 允许新增 prefab：
- 允许新增配置：
- 允许新增测试场景对象：
- 允许新增调试入口：
- 允许新增日志：
- 允许新增文档：

### 4.2.2 允许修改
- 允许修改脚本：
- 允许修改 prefab：
- 允许修改场景：
- 允许修改配置：
- 允许补充注释的文件：
- 允许补充文档的范围：

## 4.3 本次禁止改动
- 禁止修改的 Base 层：
- 禁止修改的主链模块：
- 禁止删除的现有结构：
- 禁止绕过的统一入口：
- 禁止私自新增的系统依赖：
- 禁止私自重命名的关键类：
- 禁止直接写死的内容：
- 禁止本次顺手重构的区域：

## 4.4 本次开发详细内容

### 4.4.1 功能实现
- 本次要新增的功能：
- 本次要补齐的逻辑：
- 本次要接通的调用链：
- 本次需要打通的输入输出：
- 本次是否需要 Stub / 占位实现：

### 4.4.2 数据结构
- 本次新增的数据结构：
- 本次新增的 RuntimeState：
- 本次新增的 DTO / Config：
- 本次新增字段：
- 字段的写入方：
- 字段的读取方：
- 哪些字段是运行时可变：
- 哪些字段必须只读：

### 4.4.3 架构接入
- 本次模块由谁创建：
- 本次模块由谁持有：
- 本次模块何时初始化：
- 本次模块何时释放：
- 本次模块接入哪个 Manager / Feature：
- 本次是否需要事件监听：
- 本次是否需要网络同步：
- 同步由谁发起：
- 权威写入发生在哪一侧：

### 4.4.4 UI / 交互
- 本次是否涉及 UI：
- UI 需要显示哪些字段：
- UI 允许哪些输入：
- UI 调用哪个控制层：
- 刷新由什么触发：
- 本次是否需要调试按钮 / 调试面板：
- 本次是否需要 Debug 可视反馈：

### 4.4.5 配置与资源
- 本次需要新增的配置项：
- 配置命名规范：
- 配置读取入口：
- 是否允许临时写死：
- 资源放置路径：
- prefab / 配置 / 场景命名规范：

### 4.4.6 日志与调试
- 本次必须输出的日志：
- 本次必须暴露的调试入口：
- 本次调试信息要能在哪里看到：
- 本次是否需要测试专用按钮：
- 本次是否需要测试专用场景节点：

### 4.4.7 注释与文档
- 本次必须补充的中文注释：
- 本次必须更新的文档：
- 本次开发后是否需要回写到 TASK 文档：
- 本次是否需要新增 FAQ / 已知问题说明：

---

# 5. 系统职责拆分

## 5.1 本次涉及类 / 模块职责表

| 类/模块名 | 所属层 | 本次职责 | 本次不负责 | 持有者 | 生命周期 |
|----------|--------|----------|------------|--------|----------|
|          |        |          |            |        |          |

## 5.2 调用链说明
- 谁创建谁：
- 谁持有谁：
- 谁调用谁：
- 谁负责写状态：
- 谁负责读状态：
- 谁负责刷新 UI：
- 谁负责日志输出：
- 谁负责测试入口：

## 5.3 数据流说明
- 数据从哪里来：
- 数据进入哪个模块：
- 数据在哪个时机写入：
- 数据最终被谁消费：
- UI 从哪里读：
- 是否需要服务器回传：
- 是否需要 ExplainLog / DebugLog：

---

# 6. 实现要求

## 6.1 命名与结构要求
- 脚本命名规则：
- 文件夹命名规则：
- prefab 命名规则：
- 配置命名规则：
- 测试用例命名规则：
- 日志前缀规范：

## 6.2 代码实现要求
- 必须遵守的代码风格：
- 必须避免的写法：
- 必须拆分的方法层级：
- 是否允许临时 TODO：
- 哪些地方必须写注释：
- 哪些逻辑必须可测试：
- 哪些逻辑必须可观察：

## 6.3 网络 / 状态要求（如不涉及可写“不涉及”）
- 是否涉及网络：
- 是否涉及 Server Authoritative：
- 哪些状态只能服务器写：
- 哪些内容客户端只读：
- 是否需要 Command / Rpc / Sync：
- 是否允许客户端预测：
- 状态归属在哪个 RuntimeState：

## 6.4 性能 / 稳定性要求
- 当前阶段最低性能要求：
- 当前阶段最低稳定性要求：
- 是否允许轮询：
- 是否允许临时缓存：
- 是否允许重复初始化：
- 必须避免的明显问题：

---

# 7. AI 输出要求

> AI 完成开发后，必须严格按下面结构交付，不允许只给代码不交代上下文。

## 7.1 AI 输出必须包含
1. 本次改动摘要
2. 新增文件清单
3. 修改文件清单
4. 删除文件清单（若无写“无”）
5. 挂载 / 接线说明
6. 核心类职责说明
7. 调用链说明
8. 数据流说明
9. 配置变更说明
10. 测试步骤说明
11. 最小测试结果
12. 已知限制
13. 文档同步结果
14. 后续建议承接项

## 7.2 AI 输出标准格式

### A. 本次改动摘要
- 本次完成内容：
- 本次未完成内容：
- 本次未做原因：
- 本次是否越界修改：是 / 否
- 若有越界，说明原因：

### B. 新增文件清单
| 文件路径 | 文件名 | 类型 | 用途 | 所属层 |
|---------|--------|------|------|--------|
|         |        |      |      |        |

### C. 修改文件清单
| 文件路径 | 文件名 | 修改点 | 修改原因 |
|---------|--------|--------|----------|
|         |        |        |          |

### D. 删除文件清单
| 文件路径 | 文件名 | 删除原因 | 是否确认安全 |
|---------|--------|----------|--------------|
|         |        |          |              |

### E. 挂载 / 接线说明
- 哪些对象需要挂脚本：
- 脚本挂在哪些节点：
- Inspector 需要配置哪些字段：
- 必须手动拖拽哪些引用：
- 若不配置会出现什么问题：

### F. 核心类职责说明
- 类名：
  - 负责：
  - 不负责：
  - 依赖：
  - 被谁调用：

### G. 调用链说明
- 主调用链：
- 初始化链：
- 刷新链：
- 测试调用链：
- 销毁链：

### H. 数据流说明
- 输入源：
- 逻辑处理节点：
- 状态写入点：
- UI 读取点：
- Debug 输出点：

### I. 配置变更说明
- 本次新增配置：
- 本次修改配置：
- 本次配置读取入口：
- 是否存在临时写死项：

### J. 测试步骤说明
- 使用 Scene：
- 测试前准备：
- 操作步骤：
- 预期现象：
- 可观察结果：

### K. 已知限制
- 当前仍为占位实现的部分：
- 当前未覆盖的边界：
- 当前需要后续 TASK 接上的部分：
- 当前不建议立即扩展的方向：

### L. 文档同步结果
- 更新了哪些 TASK 文档：
- 更新了哪些模块规范：
- 更新了哪些测试文档：
- 是否已补中文注释：

---

# 8. 开发完成后的整体变更记录

## 8.1 代码层变更
- 新增类：
- 修改类：
- 删除类：
- 重构关系：
- 新增接口：
- 新增枚举：
- 新增常量：

## 8.2 架构层变更
- 是否新增模块：
- 是否新增 Manager 依赖：
- 是否新增初始化链：
- 是否新增事件链：
- 是否改变调用方向：
- 是否改变模块边界：
- 是否触碰冻结层：

## 8.3 场景层变更
- 修改了哪些场景：
- 新增了哪些节点：
- 新增了哪些调试按钮：
- 新增了哪些测试入口：
- 是否新增测试场景物体挂载说明：

## 8.4 资源层变更
- 新增 prefab：
- 新增配置资产：
- 新增图集 / 音频 / 资源引用：
- 新增测试资源：

## 8.5 文档层变更
- 更新文档：
- 新增文档：
- 删除文档：
- 是否补充已知问题：
- 是否补充后续开发建议：

---

# 9. 最小测试用例

> 每个 TASK 必须至少提供可在 Unity 中执行的最小测试闭环。

## 9.1 测试目标
- 本 TASK 最低要验证什么：
- 本 TASK 本轮不验证什么：
- 本 TASK 哪些复杂情况后续再测：

## 9.2 测试环境
- Unity 版本：
- 测试 Scene：
- 依赖脚本：
- 依赖 prefab：
- 依赖配置：
- 依赖节点：
- 测试机器说明（如需要）：
- 是否需要多开：
- 是否需要 Host / Client：
- 是否需要网络连接：

## 9.3 测试等级说明
- T0：脚本级自检
- T1：场景级最小联通
- T2：模块级交互验证
- T3：主链路集成验证

## 9.4 测试用例列表

### [TC-Axx-001] 用例名称
- 测试等级：
- 目标：
- 前置条件：
- 操作步骤：
  1.
  2.
  3.
- 预期结果：
- 可观察结果：
- 失败表现：
- 涉及脚本：
- 涉及对象：
- 备注：

### [TC-Axx-002] 用例名称
- 测试等级：
- 目标：
- 前置条件：
- 操作步骤：
  1.
  2.
  3.
- 预期结果：
- 可观察结果：
- 失败表现：
- 涉及脚本：
- 涉及对象：
- 备注：

### [TC-Axx-003] 用例名称
- 测试等级：
- 目标：
- 前置条件：
- 操作步骤：
  1.
  2.
  3.
- 预期结果：
- 可观察结果：
- 失败表现：
- 涉及脚本：
- 涉及对象：
- 备注：

## 9.5 最小通过标准
- 本 TASK 至少必须通过的测试用例：
- 若哪些测试失败则视为未完成：
- 哪些问题允许保留到下个 TASK：
- 哪些问题绝不允许存在：

## 9.6 测试结果记录
| 用例编号 | 是否通过 | 实际结果摘要 | 问题记录 | 是否阻塞验收 |
|---------|----------|-------------|---------|-------------|
|         |          |             |         |             |

---

# 10. 输出来源追踪表

> 所有关键输出都必须可追踪来源，区分“明确需求”和“工程推导”。

| 输出项编号 | 输出项名称 | 输出类型 | 来源文档 | 来源章节 | 是否直接明确 | 是否工程推导 | 推导理由 | 落地文件/对象 | 验证方式 |
|-----------|-----------|---------|---------|---------|-------------|-------------|---------|-------------|---------|
| O-01      |           |         |         |         | 是/否       | 是/否       |         |             |         |

---

# 11. 风险、边界与异常处理

## 11.1 已知风险
- 
- 
- 

## 11.2 边界情况
- 
- 
- 

## 11.3 异常处理要求
- 遇到空引用时：
- 遇到配置缺失时：
- 遇到初始化顺序异常时：
- 遇到网络状态异常时：
- 遇到 UI 引用缺失时：
- 遇到测试入口未配置时：

---

# 12. 验收与结论

## 12.1 验收检查项
- [ ] 任务目标达成
- [ ] 没有越界修改冻结层
- [ ] 新增/修改脚本编译通过
- [ ] Unity 场景可执行最小测试
- [ ] 最小测试用例达到通过标准
- [ ] 调试入口可用
- [ ] 日志可观察
- [ ] 中文注释已补齐
- [ ] 文档已同步
- [ ] 输出来源可追踪
- [ ] 已知限制已说明

## 12.2 验收结果
- 验收结论：通过 / 不通过 / 部分通过
- 验收说明：
- 阻塞问题：
- 建议处理方式：

## 12.3 未完成项
- 
- 
- 

## 12.4 后续建议承接
- 建议下一个 TASK：
- 建议先补的文档：
- 建议先补的测试：
- 建议暂缓的方向：

---

# 13. 附录（可选）

## 13.1 相关配置样例
```json
{
  "example": "value"
}